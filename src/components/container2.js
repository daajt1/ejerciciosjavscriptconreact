import React from 'react'


const email = 
  `Hello ${firstname}, thanks of ordering ${qty}    
   tickets to ${evento}
   Order details
   ${firstname} ${middlename} ${lastname} 
    ${total} = $${qty*price} to ${evento} 
  ` 

  function logCompliments(result,firsname) {
      return (
          <div>
        if (result == 10){   
        console.log(`You are doing great" ${firstname}`)
    } else 
        console.log("You are so wrong")
        
        </div> )
  }


  function mostrarTicket(ticketAgent) {
      <section>
          <header>
              <h1>React ticket calculate</h1>              
          </header>
          <article>
               <h2>${article.title}</h2>
               ${article.body}
          </article>
      </section>
  }